<Window x:Class="Pharmacy1.AddCompanyWin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pharmacy1.Data" 
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="AddCompany" Height="450" Width="600"
        >
    <Window.Resources>
        <local:Company x:Key="Company"/>

        <Style TargetType="TextBox" >
            <Setter Property="Margin" Value="10,0,10,15"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>

        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="Margin" Value="10,5,10,5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment"  Value="Left"/>
        </Style>

    </Window.Resources>
    
    <Grid Margin="5" DataContext="{StaticResource Company}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
            
        </Grid.ColumnDefinitions>
                
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        

        <Border Grid.Row="0" Grid.ColumnSpan="5" Background="{StaticResource PrimaryColor}" CornerRadius="3" Margin="0,0,0,20">
            <TextBlock Text="Add New Company" 
            FontSize="24" 
            Foreground="White" 
            HorizontalAlignment="Center" 
            Padding="20,10"
            FontWeight="SemiBold"/>
        </Border>

        <TextBlock  Grid.Row="1" Grid.Column="0" Text="Code"/>
        <TextBlock  Grid.Row="2" Grid.Column="0" Text="NameEn"/>
        <TextBlock  Grid.Row="3" Grid.Column="0" Text="NameAr"/>
        <TextBlock  Grid.Row="4" Grid.Column="0" Text="Address"/>
        <TextBlock  Grid.Row="5" Grid.Column="0" Text="Phone"/>
        
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="Code" Text="{Binding Code}"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="NameEn" Text="{Binding NameEn}"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="NameAr" Text="{Binding NameAr}"/>
        <TextBox Grid.Row="4" Grid.Column="1" x:Name="Address" Text="{Binding Address}"/>
        <TextBox Grid.Row="5" Grid.Column="1" x:Name="Phone" Text="{Binding Phone}"/>

        <Button Grid.Row="6" Grid.ColumnSpan="2" 
                Content="Add" 
                Click="Button_Click"
                HorizontalAlignment="Center"
                Width="150"
                Height="30"
                Margin="0,20"/>
        
    </Grid>
</Window>
